# Mushroom Yield Forecasting

## Environment Setup

This project uses an isolated virtual environment (`.venv`) to manage dependencies.

### Activation and Verification Steps
Run the following steps in your VS Code PowerShell terminal to run the project pipeline:

1. **Verify Environment Packages:**
   ```powershell
   .\.venv\Scripts\python.exe -m pip list